var getName = require( "./getname" );

describe( "Specs for client.js file", function() {

    it( "Checks a string for an empty value", function() {
        expect( "" ).toBeEqual( "" );
    } );

} );